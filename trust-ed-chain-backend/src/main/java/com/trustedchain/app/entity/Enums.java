package com.trustedchain.app.entity;

public class Enums {
    public enum Role { STUDENT, MENTOR, ADMIN, INVESTOR }
    public enum CommunityScope { FRIENDS, INSTITUTION }
    public enum LoanStatus { PENDING, MENTOR_APPROVED, APPROVED, FUNDED, REPAID }
    public enum ViewRequestStatus { PENDING, APPROVED }
}
